{# {% extends 'base.html.twig' %} #}
{# {% block title %}New Comment{% endblock %} #}

{% block body %}
    <h5>Create new Comment</h5>

    {{ include('comment/_form.html.twig') }}
    {# Comments CRUD -> dont need it #}
    {# <a href="{{ path('comment_index') }}">back to list</a> #}
{% endblock %}
